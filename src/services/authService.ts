
import { loginUser, recoverPassword } from "@/api/auth/authApi";

// These functions now act as a facade to the actual API calls
// They can be expanded later to handle more complex logic or caching

export { loginUser, recoverPassword };
